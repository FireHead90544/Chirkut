# Database Connection URL
# For PostgreSQL, use the format: postgresql://username:password@host:port/database
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Database Reset PIN
# This PIN is required to clear all expenses and settlements (keeps users intact)
DB_RESET_PIN="000000"
